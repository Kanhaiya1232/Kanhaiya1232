#include<stdio.h>  
int main()  
{
    int i,j,user,count=0;
    printf("Enter the number");
    scanf("%d",&user);
    for(i=2;i<=j;i++)
    {
        if(user%i==0)
        {
        break;
        }
    }
    if(user==i)
    printf("No is prime no");
        else
        printf("No is not prime no");
        return 0;
    }
